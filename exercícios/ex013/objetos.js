// objetos

const pessoa = {
    nome: 'phillip',
    sobrenome: 'ferreira',
    idade: 40
}
//console.log(pessoa.nome, pessoa.sobrenome, pessoa.idade);

function soma(a, b){
    const soma = a + b;
    return soma;
}
soma(3, 4)
console.log(soma);